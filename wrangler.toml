name = "cornermash"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./"

# DO binding: the class lives in the "cornermash-do" Worker.
# Pages Functions access it via env.RATINGS_DO at runtime.
[[durable_objects.bindings]]
name = "RATINGS_DO"
class_name = "RatingsDO"
script_name = "cornermash-do"

# Old KV binding — leave commented while you verify DO works.
# [[kv_namespaces]]
# binding = "RATINGS"
# id = "..."