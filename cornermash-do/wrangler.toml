name = "cornermash-do"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Free plan: use new_sqlite_classes.
# The RatingsDO code uses state.storage.put/get which works
# transparently on top of SQLite.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["RatingsDO"]